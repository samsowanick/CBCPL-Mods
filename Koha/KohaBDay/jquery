//KohaBDayConfetti
$(document).ready(function() {
  if($('body').is('#main_intranet-main')) {  
    if ($('#news27 > h4:contains("CBCPLs Koha-versary")').length > 0) {  //update the selector to reflect the appropriate News entry.
      const canvas = document.getElementById('custom_canvas');
      const jsConfetti = new JSConfetti({ canvas });

      setTimeout(() => {
        jsConfetti.addConfetti({
          emojis: ['🎈', '✨', '🎊', '⭐', '💥', '🎉','🦄','⬛'],
          emojiSize: 70,
          confettiNumber: 99,
        });
      }, 500);
    } 
  } 
});
